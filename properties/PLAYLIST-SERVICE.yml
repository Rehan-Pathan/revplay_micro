server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/revplay_playlist_db
    username: revature
    password: dharam

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

logging:
  level:
    org.springframework.security: DEBUG
    com.revplay: DEBUG